import styled from 'styled-components';

export const FooterContainer = styled.footer`
    text-align: center;
    padding: 5rem 0 2rem;
    color: #000;
    opacity: 0.6;
    font-size: 2rem;

    p:first-child {
        font-weight: 200;
        color: #fff;
    }
`